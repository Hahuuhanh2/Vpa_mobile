package y;

import android.util.Pair;
import f0.m0;
import java.util.HashSet;
import java.util.Locale;

/* compiled from: FlashAvailabilityBufferUnderflowQuirk */
public class p implements m0 {

    /* renamed from: a  reason: collision with root package name */
    public static final HashSet f17346a;

    static {
        HashSet hashSet = new HashSet();
        f17346a = hashSet;
        Locale locale = Locale.US;
        hashSet.add(new Pair("sprd".toLowerCase(locale), "lemp".toLowerCase(locale)));
        hashSet.add(new Pair("sprd".toLowerCase(locale), "DM20C".toLowerCase(locale)));
    }
}
